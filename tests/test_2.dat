//fst = 0; // все типы int
snd = 1;
iters = ?;

while (iter > 0) {
    tmp = fst;
    fst = snd;
    snd = snd + tmp;
    iters = iters - 1;
    a = 9;
}

print snd; 
print a;


// проблема с областью видимости : то что в цикле все считается за 0 даже для глобальных переменных